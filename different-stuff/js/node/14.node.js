var cities = [...],
    _cities = [...],
    obj = {...},
    _obj = {...};

console.log(
  a_all: [...cities, ..._cities],
  o_all: {...obj, ..._obj});

var divs = window.document.querySelector('div');

console.log(
  [...divs].map(function(el) {
    return el;
  }));
